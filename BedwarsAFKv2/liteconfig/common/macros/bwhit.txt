do()
	if((%HITID% = -1)||(@#distancefrom <= 2.5))
		do()
			keyup(forward)
			keydown(back)
		until(@#distancefrom >= 2.5)
			keyup(back)
			keydown(forward)
			keydown(sprint)
	else
		keydown(forward)
	endif;
loop;
